/**
 * Schema markup is now embedded directly in index.html for better
 * crawler visibility. This component is kept as a no-op so existing
 * imports don't break.
 */
export default function SchemaMarkup() {
  return null;
}
